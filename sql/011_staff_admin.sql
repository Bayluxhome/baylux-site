-- Динамические админ-права для сотрудников. Главный админ (из конфига) выдаёт/снимает.
-- Флаг запекается в сессию при входе пользователя (вступает в силу при следующем входе).
alter table site_users add column if not exists is_admin boolean default false;
